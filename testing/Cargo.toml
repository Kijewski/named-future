[package]
name = "named-future-tests"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
named-future = { version = "=0.1.0-pre.2", path = ".." }

[dev-dependencies]
futures-lite = "2.2.0"
trybuild = { version = "1.0.80", features = ["diff"] }
version_check = "0.9.4"
